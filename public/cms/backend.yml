# Sveltia-only override. Loaded after /admin/config.yml to reuse the exact field model.
# GitHub OAuth provider must be linked to the Netlify site before this route is used.
backend:
  name: github
  repo: UofT-FoM-GRC/Website
  # Temporary PR rehearsal target. Restore to dev before merge.
  branch: feat/modern-cms-handoff
  auth_methods: [oauth]
publish_mode: null
